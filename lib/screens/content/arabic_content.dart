import 'package:flutter/material.dart';

class ArabicContent extends StatelessWidget {
  const ArabicContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Arabic'),

      ),
    );
  }
}
