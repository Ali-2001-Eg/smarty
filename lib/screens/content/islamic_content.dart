import 'package:flutter/material.dart';

class IslamicContent extends StatelessWidget {
  const IslamicContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Islamic'),
      ),
    );
  }
}
