import 'package:flutter/material.dart';

class EnglishContent extends StatelessWidget {
  const EnglishContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('English'),
      ),
    );
  }
}
