import 'package:flutter/material.dart';

class MathContent extends StatelessWidget {
  const MathContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Math'),

      ),
    );
  }
}
