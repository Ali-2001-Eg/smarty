import 'package:flutter/material.dart';
class GameItems extends StatelessWidget {
  final String imagePath;
  final String title;
  final String url;
  GameItems(this.imagePath, this.title,this.url);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
